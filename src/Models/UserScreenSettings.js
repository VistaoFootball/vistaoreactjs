export class UserScreenSettings {
  constructor(user, bg_color, side_bar_mini, mode) {
    this.user = user;
    this.bg_color = bg_color;
    this.side_bar_mini = side_bar_mini;
    this.mode = mode;
  }
}
